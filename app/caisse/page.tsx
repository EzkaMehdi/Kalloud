"use client";
import { CalendarPlus, Plus, Table2 } from "lucide-react";
import { useEffect, useState } from "react";
import { CashMovementModal } from "../../components/cash-movement-modal";
import { CloseDayModal } from "../../components/close-day-modal";
import { OrderDrawer } from "../../components/order-drawer";
import { Shell } from "../../components/shell";
const initial=[{id:1,name:"Table 1",status:"FREE"},{id:2,name:"Table 2",status:"FREE"},{id:3,name:"Table 3",status:"FREE"},{id:4,name:"Table 4",status:"FREE"},{id:5,name:"Table 5",status:"FREE"},{id:6,name:"Table 6",status:"FREE"},{id:7,name:"Table 7",status:"FREE"},{id:8,name:"Comptoir / Vente directe",status:"FREE"}];
export default function Caisse(){const [tables,setTables]=useState(initial);const [selected,setSelected]=useState<{id:number|null,name:string}|null>(null);const [notice,setNotice]=useState("");const [movementOpen,setMovementOpen]=useState(false);const [closeOpen,setCloseOpen]=useState(false);const [cash,setCash]=useState(150);const [revenue,setRevenue]=useState(0);const [now,setNow]=useState(new Date());useEffect(()=>{const id=setInterval(()=>setNow(new Date()),30000);fetch("http://localhost:3001/api/tables").then(r=>r.json()).then(setTables).catch(()=>{});fetch("http://localhost:3001/api/cash-summary").then(r=>r.json()).then(d=>setCash(Number(d.balance))).catch(()=>{});fetch("http://localhost:3001/api/dashboard").then(r=>r.json()).then(d=>setRevenue(Number(d.revenue))).catch(()=>{});return()=>clearInterval(id)},[]);function message(text:string){setNotice(text);setTimeout(()=>setNotice(""),3500)}function done(total:number){if(selected){setTables(t=>t.map(x=>x.id===selected.id?{...x,status:"FREE"}:x));setSelected(null);setRevenue(c=>c+total);message("Vente encaissée, stock et chiffre d’affaires mis à jour")}}function movementSaved(amount:number,type:"IN"|"OUT"){setCash(c=>c+(type==="IN"?amount:-amount));message("Mouvement enregistré dans le journal de caisse")}function newDay(opening:number){setCash(opening);setRevenue(0);message("Nouvelle journée ouverte : le bilan précédent est clôturé")};return <Shell><div className="page-head"><div><p className="eyebrow">{new Intl.DateTimeFormat("fr-FR",{weekday:"long",day:"numeric",month:"long",hour:"2-digit",minute:"2-digit"}).format(now)}</p><h1>La caisse</h1></div><button onClick={()=>setSelected({id:null,name:"Vente directe"})} className="icon-button" aria-label="Vente directe"><Plus size={22}/></button></div>{notice&&<div className="status" style={{marginBottom:12}}><span className="dot"/>{notice}</div>}<div className="cash-card"><div><small>Espèces en caisse</small><strong>{cash.toFixed(2).replace(".",",")} €</strong></div><button onClick={()=>setMovementOpen(true)}>Mouvement</button></div><div className="quick-kpi"><span>Chiffre d’affaires de la journée</span><b>{revenue.toFixed(2).replace(".",",")} €</b><small>Une journée se clôture manuellement</small></div><button className="close-day-button" onClick={()=>setCloseOpen(true)}><CalendarPlus size={18}/><span><b>Nouvelle journée</b><small>Clôturer le service actuel et repartir sur un nouveau bilan</small></span></button><div className="section-title"><div><h2>Plan de salle</h2><p className="eyebrow">Touchez une table pour prendre la commande</p></div><span className="status"><span className="dot"/>Journée ouverte</span></div><div className="tables">{tables.map(t=><button onClick={()=>{setSelected({id:t.id,name:t.name});setTables(all=>all.map(x=>x.id===t.id?{...x,status:"OCCUPIED"}:x));fetch(`http://localhost:3001/api/tables/${t.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:"OCCUPIED"})})}} key={t.name} className={`table-card ${t.status==="OCCUPIED"?"occupied":""}`}><Table2 className="table-icon" size={25}/><span className={`pill ${t.status==="FREE"?"free":"busy"}`}>{t.status==="FREE"?"LIBRE":"EN COURS"}</span><h3>{t.name}</h3><p>{t.status==="FREE"?"Disponible":"Ticket en cours"}</p></button>)}</div>{selected&&<OrderDrawer table={selected.name} tableId={selected.id} onClose={()=>setSelected(null)} onComplete={done}/>} {movementOpen&&<CashMovementModal onClose={()=>setMovementOpen(false)} onSaved={movementSaved}/>} {closeOpen&&<CloseDayModal onClose={()=>setCloseOpen(false)} onFinished={newDay}/>}</Shell>}
